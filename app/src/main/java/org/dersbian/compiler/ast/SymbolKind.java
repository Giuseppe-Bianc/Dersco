package org.dersbian.compiler.ast;

/** The category of an entity in a semantic symbol table. */
public enum SymbolKind {
    VARIABLE,
    PARAMETER,
    FUNCTION,
    CLASS,
    FIELD,
    METHOD
}
