/**
 * Parsing phase for the Dersco language.
 *
 * <p>This package owns the parser and must not import {@code compiler.semantic} or {@code
 * compiler.codegen} packages.
 */
package org.dersbian.compiler.syntax;
